# Community Plugins

Place custom plugins here. Each plugin is a subdirectory containing at least
`plugin.py` and `__init__.py`.

See `plugins/README.md` for how to write a plugin.

This directory is gitignored — plugins you add here will not be tracked in version control.